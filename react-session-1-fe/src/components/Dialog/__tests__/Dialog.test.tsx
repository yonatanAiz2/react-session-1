import Dialog from "../Dialog";

describe("Dialog", () => {
  it("Dialog should be visible when isOpen prop is true", () => {});
  it("When clicking submit, should fire onSubmit function", () => {});
  it("When clicking cancel, should fire onCancel function", () => {});
});
